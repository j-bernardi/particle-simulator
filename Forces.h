//Here begins file Forces.h (see file Forces.cc)

#include <vector>
class Forces {
public:
    vector<double> forceCalculator(vector<double> xVector, int dimension, double constA, float powerDep);
};
